<div class="w-full max-w-md mt-10 mb-32">
  <div class="pt-4 pb-5">
    <h1 class="font-bold mb-6"><%= t('.title') %></h1>
    <% if @feedbacks.present? %>
      <%= render @feedbacks %>
    <% else %>
      <p><%= t('.no_result') %></p>
    <% end %>
  </div>
</div>
